# ============================================================
# Cortex Enterprise — Environment Variables
# ============================================================
# Copy to .env:  cp .env.example .env
# Then fill in your values below.
# ============================================================

# --- AUTHENTICATION ---
#
# Cortex uses your Anthropic subscription (Claude Max / Team / Enterprise).
# Just run `claude login` once to authenticate via browser — no API key needed.
#
# If you prefer API key auth instead, uncomment and set:
# ANTHROPIC_API_KEY=sk-ant-...

# Claude CLI path (auto-detected if in PATH)
# CLAUDE_PATH=claude

# --- OPTIONAL: Slack ---

# Bot token from Slack App (starts with xoxb-)
# SLACK_BOT_TOKEN=xoxb-...

# App-level token for Socket Mode (starts with xapp-)
# SLACK_APP_TOKEN=xapp-...

# --- OPTIONAL: Microsoft Teams ---

# Azure Bot registration App ID
# MSTEAMS_APP_ID=

# Azure Bot registration Password
# MSTEAMS_APP_PASSWORD=

# --- OPTIONAL: Webhooks ---

# Secret for validating inbound webhooks (use: openssl rand -base64 32)
# WEBHOOK_SECRET=

# --- OPTIONAL: Supabase (persistent memory) ---

# Supabase project URL (looks like https://abc123.supabase.co)
# SUPABASE_URL=

# Supabase anon public key (starts with eyJ...)
# SUPABASE_ANON_KEY=

# --- RECOMMENDED: Personalization ---

# Bot operator name
# USER_NAME=

# Timezone (IANA format: America/New_York, Europe/Berlin, Asia/Jerusalem, etc.)
# USER_TIMEZONE=UTC

# --- OPTIONAL: Paths ---

# Working directory for Claude (defaults to ~/workspace)
# PROJECT_DIR=

# Relay data directory (defaults to ~/.cortex-relay)
# RELAY_DIR=

# --- RELAY SETTINGS ---

# Port for webhook server (default: 18789)
# PORT=18789

# Default team agent for routing (default: orchestrator)
# DEFAULT_AGENT=orchestrator
